# Assumptions

1. The existing demo source vault at `demo/obsidian-ops/vault` is the best baseline for runtime copies.
2. Runtime demo artifacts should remain in `.scratch/` so they are easy to inspect and reset without touching the source vault.
3. The user will run the demo through `devenv shell -- ...`.
