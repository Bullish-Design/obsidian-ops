# Clean URL Routing — Plan

## ABSOLUTE RULE

- NO SUBAGENTS.

1. Add clean-URL middleware to `src/obsidian_ops/app.py`.
2. Add tests in `tests/test_api.py` covering extensionless file and directory paths.
3. Run `devenv shell -- pytest tests/test_api.py -v` then full test suite.
4. Update tracking files and commit/push.

## ABSOLUTE RULE

- NO SUBAGENTS.
