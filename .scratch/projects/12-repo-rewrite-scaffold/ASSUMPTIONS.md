# Assumptions

- User wants a hard reset of repository implementation code.
- Keep scaffolding and project-level config files (for example `devenv.nix`, `pyproject.toml`, and related root files).
- Preserve demo artifacts under `demo/`.
- Keep empty source/test directory structure so rewrite can start cleanly.
