# Context

The repository has been switched to branch `refactor` and implementation/test code has been removed. `demo/` remains intact. Top-level scaffold/config files remain intact (`devenv.nix`, `devenv.yaml`, `devenv.lock`, `pyproject.toml`, `uv.lock`, `README.md`, `AGENTS.md`). Source and test directories are kept with `.gitkeep` placeholders for clean-slate rewrite work. These changes are committed as `07a7f85` with message: `chore: reset codebase to scaffold for refactor`.
