# Project 17 Plan: Main + Minor Release Tag

1. Move `main` to the current `refactor` state.
2. Bump project version minor (`0.1.0` -> `0.2.0`).
3. Commit the version bump on `main`.
4. Create a release tag for the new version.
5. Push `main` and the tag.
