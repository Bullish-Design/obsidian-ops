# Demo Scaffold — Assumptions

- User wants the same style of demo setup used in `ssg-gen`.
- "Functional demo" means runnable from `devenv shell -- demo run` and `demo/obsidian-ops/run_demo.sh`.
- Existing `obsidian-ops` app startup and API routes remain the runtime core.
