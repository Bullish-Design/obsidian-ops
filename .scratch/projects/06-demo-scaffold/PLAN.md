# Demo Scaffold — Plan

## ABSOLUTE RULE

- NO SUBAGENTS.

1. Study `ssg-gen` demo shape and map equivalent workflow for `obsidian-ops`.
2. Add `demo/obsidian-ops/` scaffold files (`README.md`, `run_demo.sh`, `vault/...`).
3. Implement `src/obsidian_ops/demo_cli.py` with `demo run|serve|cleanup`.
4. Wire CLI entrypoint in `pyproject.toml`.
5. Verify with `devenv shell -- uv sync --extra dev`, `devenv shell -- demo --help`, and `devenv shell -- demo run` smoke test.
6. Update project tracking files and commit/push.

## ABSOLUTE RULE

- NO SUBAGENTS.
