# Assumptions

1. A "key press to continue" can be implemented as pressing Enter in terminal mode.
2. `scripts/live_vault_demo.py` step order is the source of truth for `DEMO_SCRIPT.md`.
3. Demo users will keep Obsidian open to the runtime vault while running the script.
