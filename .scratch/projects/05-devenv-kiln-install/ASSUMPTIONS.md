# Devenv Kiln Install — Assumptions

- Goal is to replicate kiln installation method from `/home/andrew/Documents/Projects/ssg-gen/`.
- Source of truth for method is `ssg-gen`'s `devenv.nix` and `devenv.yaml`.
- Keep existing devenv behavior and add kiln support with minimal changes.
