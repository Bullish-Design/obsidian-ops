# Context

Implementation has begun against `REFACTOR_GUIDE.md` on branch `refactor`. Current objective is to execute Steps 0-11 in order with validation after each step, committing and pushing each completed step. Step 0 is implemented and validated (`uv sync`, `pytest`, `ruff`). Next step is Step 1 (`errors.py` exception hierarchy).
